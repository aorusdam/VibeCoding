MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority
MONGODB_DB=cartwise
# Optional: enables responses from OpenAI. Without it, the assistant uses product-aware recommendations.
OPENAI_API_KEY=
