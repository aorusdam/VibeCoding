"use client";
import { useSearchParams, Link } from "next/navigation";
import { useState } from "react";
export default function Checkout() { const params = useSearchParams(); const [done, setDone] = useState(""); const total = Number(params.get("total") || 0); async function submit(e) { e.preventDefault(); const f = new FormData(e.currentTarget); const customer = Object.fromEntries(f); const response = await fetch("/api/checkout", { method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({ customer, items:[{ name:"Cartwise order", quantity:1 }], total }) }); const data = await response.json(); setDone(data.orderNumber || data.error); }
  if (done) return <main className="checkout"><Link href="/" className="brand">cartwise</Link><section className="confirmation"><p className="eyebrow">Order confirmed</p><h1>Thank you for shopping thoughtfully.</h1><p>Your confirmation number is <b>{done}</b>. We’ll send the details to your email shortly.</p><Link className="button dark" href="/">Back to shop</Link></section></main>;
  return <main className="checkout"><Link href="/" className="brand">cartwise</Link><div className="checkout-grid"><section><p className="eyebrow">Checkout</p><h1>A few details, then you’re all set.</h1><form onSubmit={submit}><label>Full name<input name="name" required placeholder="Alex Morgan"/></label><label>Email<input name="email" type="email" required placeholder="alex@example.com"/></label><label>Shipping address<input name="address" required placeholder="123 Market Street"/></label><button className="button dark" type="submit">Place order <span>→</span></button></form></section><aside className="order-card"><p>ORDER SUMMARY</p><div><span>Cartwise order</span><b>${total.toFixed(2)}</b></div><div><span>Shipping</span><b>Free</b></div><hr/><strong><span>Total</span><span>${total.toFixed(2)}</span></strong></aside></div></main>;
}
